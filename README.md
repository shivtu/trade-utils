## Misc helper functions used for trading

`npm i trade-utils`

Example:

`import { runEveryNew5thMin } from 'trade-utils';`

`runEveryNew5thMin(() => console.log('Check if 30SMA crosses 50SMA every 5 mins'))`
